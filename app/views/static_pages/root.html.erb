<script type="text/javascript">
  <% if logged_in?  %>
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>
  <% end  %>
  window.logoImg = "<%= image_url('vf-logo.png') %>"
  window.coverImg = "<%= image_url('mainPageCover.jpg') %>"
</script>
<main id="root"></main>
